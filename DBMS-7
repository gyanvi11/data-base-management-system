create database TCL;
use TCL;
create table TCL(ID INT,NAME VARCHAR(30),PAYMENT INT);
insert into TCL values(23,'RAM',500); 
SELECT * FROM TCL;START transaction;
insert into TCL VALUES(64,'RAM',0);
rollback;
